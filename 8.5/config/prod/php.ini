; Production overrides, loaded after the base php.ini and extension ini files.
[PHP]
memory_limit=256M
display_errors=off
display_startup_errors=off

; php.ini-production standard: assert() code is compiled out entirely.
zend.assertions=-1
