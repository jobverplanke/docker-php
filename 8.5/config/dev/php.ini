; Development overrides, loaded after the base php.ini and extension ini files.
[PHP]
memory_limit=256M
display_errors=on
display_startup_errors=on

; php.ini-development standard: assert() is active in dev.
zend.assertions=1
