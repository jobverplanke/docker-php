[opcache]
; Development: check for updated code on every request.
opcache.validate_timestamps=1
opcache.revalidate_freq=0

; xdebug disables the JIT at runtime anyway; turning it off explicitly
; keeps the "JIT is incompatible with third party extensions" startup
; warning out of the logs.
opcache.jit=off
